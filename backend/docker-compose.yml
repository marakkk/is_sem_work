services:

  postgres:
    restart: always
    image: postgres:latest
    environment:
      POSTGRES_DB: dream_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
